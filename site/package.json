{
  "name": "graphcompose-ai-flow-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prebuild": "node scripts/sync-assets.mjs && node scripts/extract-project.mjs && node scripts/extract-pipeline.mjs && node scripts/extract-templates.mjs",
    "predev": "node scripts/sync-assets.mjs && node scripts/extract-project.mjs && node scripts/extract-pipeline.mjs && node scripts/extract-templates.mjs",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^4.16.18"
  }
}

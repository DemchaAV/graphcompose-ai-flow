# User request

Wire the CV reference template into the documented asset flow: pull
contact and social icons from Iconify, register a real Google Fonts
family (one of the GraphCompose bundled families), and read every
icon path and font name from `assets-manifest.json` instead of the
inline letter placeholders shipped in `revision-002`.

diff --git "a/examples\\invoice-reference\\revisions\\revision-002\\generated-template.java" "b/examples\\invoice-reference\\revisions\\revision-003\\generated-template.java"
index 794eae5..dcaf6cd 100644
--- "a/examples\\invoice-reference\\revisions\\revision-002\\generated-template.java"
+++ "b/examples\\invoice-reference\\revisions\\revision-003\\generated-template.java"
@@ -22,7 +22,7 @@ import com.demcha.compose.document.theme.BusinessTheme;
 
 /**
  * Invoice template generated by the Template Coder Agent for
- * revision-002 of the invoice-reference example.
+ * revision-003 of the invoice-reference example.
  *
  * <p>Patch on revision-001: the three summary rows (Subtotal,
  * Tax, TOTAL) are <strong>lifted out of the line-items table</strong>
@@ -31,18 +31,22 @@ import com.demcha.compose.document.theme.BusinessTheme;
  * {@code LineItems} table in the page flow. The Summary section uses
  * a 4-column inner table that mirrors the LineItems column proportions
  * ({@code auto / 54 / 96 / 96}) so it right-aligns visually with the
- * Amount column above. All other regions are identical to revision-001;
- * see {@code patch.diff}.</p>
+ * Amount column above.</p>
+ *
+ * <p>Patch on revision-002: the page flow now has a root padding token
+ * so the document content no longer renders flush against the page's
+ * top-left edge.</p>
  *
  * <p>Targets the real GraphCompose 1.6 canonical surface
  * ({@code com.demcha.compose.document.*}). Implements the canonical
  * {@link InvoiceTemplate} contract so the same {@link InvoiceDocumentSpec}
- * fixture drives both revisions and the upstream {@code InvoiceTemplateV2}.</p>
+ * fixture drives all example revisions and the upstream {@code InvoiceTemplateV2}.</p>
  */
 public final class GeneratedInvoiceTemplate implements InvoiceTemplate {
 
     private static final double TABLE_PADDING = 7.0;
     private static final double PAGE_SPACING = 16.0;
+    private static final double PAGE_MARGIN = 24.0;
     private static final double HERO_PADDING = 14.0;
     private static final double HERO_CORNER_RADIUS = 10.0;
 
@@ -67,18 +71,18 @@ public final class GeneratedInvoiceTemplate implements InvoiceTemplate {
 
     @Override
     public String getTemplateId() {
-        return "invoice-reference-revision-002";
+        return "invoice-reference-revision-003";
     }
 
     @Override
     public String getTemplateName() {
-        return "Invoice Reference (revision-002)";
+        return "Invoice Reference (revision-003)";
     }
 
     @Override
     public String getDescription() {
         return "Companion-repo example of the canonical invoice template flow "
-                + "(revision-002 — Summary lifted out of LineItems table).";
+                + "(revision-003 - Page margin added around the invoice content).";
     }
 
     @Override
@@ -112,6 +116,7 @@ public final class GeneratedInvoiceTemplate implements InvoiceTemplate {
         document.pageFlow(page -> page
                 .name("Invoice")
                 .spacing(PAGE_SPACING)
+                .padding(DocumentInsets.of(PAGE_MARGIN))
                 .addRow("Header", row -> renderHeader(row, data))
                 .addSection("Hero", section -> renderHero(section, data, surfaceMuted, accent))
                 .addRow("Parties", row -> renderParties(row, data))

diff --git a/examples/invoice-reference/revisions/revision-001/generated-template.java b/examples/invoice-reference/revisions/revision-002/generated-template.java
index 17742d7..794eae5 100644
--- a/examples/invoice-reference/revisions/revision-001/generated-template.java
+++ b/examples/invoice-reference/revisions/revision-002/generated-template.java
@@ -22,24 +22,22 @@ import com.demcha.compose.document.theme.BusinessTheme;
 
 /**
  * Invoice template generated by the Template Coder Agent for
- * revision-001 of the invoice-reference example. The shape follows
- * the "Preferred template shape" in the project plan (section 5.6) and
- * the architecture plan committed alongside this file.
+ * revision-002 of the invoice-reference example.
  *
- * <p>This file targets the real GraphCompose 1.6 canonical surface
- * ({@code com.demcha.compose.document.*}). It implements the canonical
- * {@link InvoiceTemplate} contract from the library so the same
- * {@link InvoiceDocumentSpec} that drives {@code InvoiceTemplateV2}
- * also drives this companion-repo example. All visual choices route
- * through a {@link BusinessTheme}: palette, text scale, table
- * preset.</p>
+ * <p>Patch on revision-001: the three summary rows (Subtotal,
+ * Tax, TOTAL) are <strong>lifted out of the line-items table</strong>
+ * (where revision-001 placed them via {@code TableBuilder.totalRow(...)})
+ * into a dedicated {@code Summary} section placed directly after the
+ * {@code LineItems} table in the page flow. The Summary section uses
+ * a 4-column inner table that mirrors the LineItems column proportions
+ * ({@code auto / 54 / 96 / 96}) so it right-aligns visually with the
+ * Amount column above. All other regions are identical to revision-001;
+ * see {@code patch.diff}.</p>
  *
- * <p>Where the original reference document called for a styling
- * choice that the real DSL does not yet expose on the
- * {@code SectionBuilder} / {@code TableBuilder} surface, the comment
- * is preserved as a {@code TODO(skill-validation):} note that names
- * the visual intent and the closest real surface — the fixture run
- * pins down the exact builder call.</p>
+ * <p>Targets the real GraphCompose 1.6 canonical surface
+ * ({@code com.demcha.compose.document.*}). Implements the canonical
+ * {@link InvoiceTemplate} contract so the same {@link InvoiceDocumentSpec}
+ * fixture drives both revisions and the upstream {@code InvoiceTemplateV2}.</p>
  */
 public final class GeneratedInvoiceTemplate implements InvoiceTemplate {
 
@@ -69,18 +67,18 @@ public final class GeneratedInvoiceTemplate implements InvoiceTemplate {
 
     @Override
     public String getTemplateId() {
-        return "invoice-reference-revision-001";
+        return "invoice-reference-revision-002";
     }
 
     @Override
     public String getTemplateName() {
-        return "Invoice Reference (revision-001)";
+        return "Invoice Reference (revision-002)";
     }
 
     @Override
     public String getDescription() {
         return "Companion-repo example of the canonical invoice template flow "
-                + "(revision-001 of the invoice-reference walkthrough).";
+                + "(revision-002 — Summary lifted out of LineItems table).";
     }
 
     @Override
@@ -117,7 +115,8 @@ public final class GeneratedInvoiceTemplate implements InvoiceTemplate {
                 .addRow("Header", row -> renderHeader(row, data))
                 .addSection("Hero", section -> renderHero(section, data, surfaceMuted, accent))
                 .addRow("Parties", row -> renderParties(row, data))
-                .addTable(table -> renderLineItems(table, data, bordered, headerStyle, totalStyle, surfaceMuted, surface))
+                .addTable(table -> renderLineItems(table, data, bordered, headerStyle, surfaceMuted, surface))
+                .addSection("Summary", section -> renderSummaryBlock(section, data, bordered, totalStyle))
                 .addSection("Footer", section -> renderFooter(section, data, accent)));
     }
 
@@ -185,16 +184,12 @@ public final class GeneratedInvoiceTemplate implements InvoiceTemplate {
                                  InvoiceData data,
                                  DocumentTableStyle bordered,
                                  DocumentTableStyle headerStyle,
-                                 DocumentTableStyle totalStyle,
                                  DocumentColor zebraOdd,
                                  DocumentColor zebraEven) {
+        // revision-002: the line-items table holds ONLY the data rows.
+        // The trailing totals/summary rows have moved to the dedicated
+        // "Summary" section composed after this table in the page flow.
         table.name("LineItems")
-                // Description gets the leftover width via auto; numeric
-                // columns are fixed so a long description never blows the
-                // table past the page's inner width. The original example
-                // expressed this as 50 / 10 / 20 / 20 fractional weights;
-                // the real DSL uses auto + fixed points (per
-                // InvoiceTemplateV2).
                 .columns(
                         DocumentTableColumn.auto(),
                         DocumentTableColumn.fixed(54),
@@ -212,19 +207,34 @@ public final class GeneratedInvoiceTemplate implements InvoiceTemplate {
                     item.unitPrice(),
                     item.amount());
         }
-        // The summary rows live in the table's totals area, mirroring
-        // the v1.6 InvoiceTemplateV2 shape: every summary row except the
-        // last renders as a plain row; the last renders via
-        // totalRow(...) so it picks up the totals style.
-        List<InvoiceSummaryRow> summaries = data.summaryRows();
-        for (int i = 0; i < summaries.size(); i++) {
-            InvoiceSummaryRow summary = summaries.get(i);
-            if (i == summaries.size() - 1) {
-                table.totalRow(totalStyle, "", "", summary.label(), summary.value());
-            } else {
-                table.row("", "", summary.label(), summary.value());
+    }
+
+    private void renderSummaryBlock(SectionBuilder section,
+                                    InvoiceData data,
+                                    DocumentTableStyle bordered,
+                                    DocumentTableStyle totalStyle) {
+        // Mirror the LineItems column proportions so the Subtotal / Tax /
+        // TOTAL rows sit visually under the Amount column above. The real
+        // TableBuilder supports the same auto + fixed-point columns; using
+        // an identical column spec is the shortest path to a shared grid.
+        section.addTable(summary -> {
+            summary.name("SummaryTable")
+                    .columns(
+                            DocumentTableColumn.auto(),
+                            DocumentTableColumn.fixed(54),
+                            DocumentTableColumn.fixed(96),
+                            DocumentTableColumn.fixed(96))
+                    .defaultCellStyle(bordered);
+            List<InvoiceSummaryRow> summaries = data.summaryRows();
+            for (int i = 0; i < summaries.size(); i++) {
+                InvoiceSummaryRow row = summaries.get(i);
+                if (i == summaries.size() - 1) {
+                    summary.totalRow(totalStyle, "", "", row.label(), row.value());
+                } else {
+                    summary.row("", "", row.label(), row.value());
+                }
             }
-        }
+        });
     }
 
     private void renderFooter(SectionBuilder section, InvoiceData data, DocumentColor accent) {
@@ -274,9 +284,6 @@ public final class GeneratedInvoiceTemplate implements InvoiceTemplate {
                 .addParagraph(p -> p
                         .text(joinAddress(party))
                         .textStyle(theme.text().body())
-                        // Multi-line address blocks need a small line gap so
-                        // address / email / phone do not run together; matches
-                        // InvoiceTemplateV2's 1.3 line spacing.
                         .lineSpacing(1.3)
                         .margin(DocumentInsets.zero()));
     }

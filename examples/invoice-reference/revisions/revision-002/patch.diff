diff --git a/examples/invoice-reference/revisions/revision-001/generated-template.java b/examples/invoice-reference/revisions/revision-002/generated-template.java
index ffefb36..84d402d 100644
--- a/examples/invoice-reference/revisions/revision-001/generated-template.java
+++ b/examples/invoice-reference/revisions/revision-002/generated-template.java
@@ -11,17 +11,13 @@ import com.demcha.graphcompose.TableBuilder;

 /**
  * Invoice template generated by the Template Coder Agent for
- * revision-001 of the invoice-reference example. The shape follows
- * the "Preferred template shape" in the project plan (§5.6) and the
- * architecture plan committed alongside this file.
- *
- * The template only uses primitives documented by the
- * graphcompose-1.6 skill pack: DocumentSession, DocumentTemplate,
- * BusinessTheme, pageFlow, addRow, addSection, addTable, and the
- * RowBuilder / SectionBuilder / TableBuilder builders. Uncertain
- * method bindings are tagged with TODO(visual-review) instead of
- * being guessed; the Phase 4 validation fixtures will confirm the
- * exact builder surface.
+ * revision-002 of the invoice-reference example. Patch on
+ * revision-001: the three summary rows (Subtotal, tax, TOTAL) are
+ * lifted out of table.footer(...) into a dedicated Summary section
+ * under the line-items table; the section mirrors the LineItems
+ * column proportions so it right-aligns with the Amount column.
+ * All other regions are identical to revision-001; see patch.diff.
+ * Uncertain method bindings are tagged TODO(visual-review).
  */
 public final class InvoiceTemplate implements DocumentTemplate<InvoiceSpec> {

@@ -45,6 +41,7 @@ public final class InvoiceTemplate implements DocumentTemplate<InvoiceSpec> {
                 .addSection("Hero", section -> renderHero(section, spec))
                 .addRow("Parties", row -> renderParties(row, spec))
                 .addTable("LineItems", table -> renderLineItems(table, spec))
+                .addSection("Summary", section -> renderSummaryBlock(section, spec))
                 .addSection("Footer", section -> renderFooter(section, spec)));
     }

@@ -140,14 +137,17 @@ public final class InvoiceTemplate implements DocumentTemplate<InvoiceSpec> {
                     .cell(item.unitPrice())
                     .cell(item.amount()));
         }
+    }

-        // The three summary rows live in a trailing section that visually
-        // aligns with the Amount column.
-        table.footer(footer -> {
-            renderSummaryRow(footer, "Subtotal", spec.subtotal(), false);
-            renderSummaryRow(footer, spec.taxLabel(), spec.tax(), false);
-            renderSummaryRow(footer, "TOTAL", spec.total(), true);
-        });
+    private void renderSummaryBlock(SectionBuilder section, InvoiceSpec spec) {
+        // TODO(visual-review): confirm the column-mirror binding between
+        // SectionBuilder and TableBuilder. Intent: share the LineItems
+        // column proportions (50/10/20/20) so Subtotal/Tax/TOTAL sit
+        // under the Amount column. Phase 4 fixtures ship the verified
+        // method name; until then the helper leans on section alignment.
+        renderSummaryRow(section, "Subtotal", spec.subtotal(), false);
+        renderSummaryRow(section, spec.taxLabel(), spec.tax(), false);
+        renderSummaryRow(section, "TOTAL", spec.total(), true);
     }

     private void renderFooter(SectionBuilder section, InvoiceSpec spec) {

# User Request - revision-006

Continue from `revision-005` and attempt the next named layer: make the top
dark surface behave like the reference, where the band visually runs behind the
left cream card.

Constraint: keep the solution semantic. Do not paint a raw PDF/canvas rectangle
just to fake the background.

# User Request

User supplied the same single-page corporate CV screenshot already tracked by
`examples/noir-corporate-cv/reference/reference.png` and asked in Russian:

```text
сделай шаблон с референса
```

Decision: continue the existing `noir-corporate-cv` project as
`revision-002` instead of creating a duplicate project. The parent revision is
`revision-001`, which was a structural draft with documented visual
substitutions for panels, bars, the CV circle, and rating dots.

# User Request - revision-005

Continuation of the layer-by-layer flow after `revision-004`.

The user explicitly corrected the workflow: the template should not stop at a
near draft. Each visual layer should be rendered, reviewed, and followed by the
next concrete patch until approval or a real blocker.

This revision targets the next visible geometry layer: the CV badge size and
vertical placement, the name/title block placement, and the main-column rhythm.
